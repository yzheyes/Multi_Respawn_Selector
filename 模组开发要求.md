我需要开发一个 Java 版 Minecraft 模组，目标版本为 Minecraft 1.20.1，模组加载器为 Fabric，使用 Yarn mappings、Fabric API、Gradle Loom 开发。

模组名称暂定为：Multi Respawn Selector
模组目标：
改进原版 Minecraft 的玩家重生机制。原版中，玩家死亡后只能在最近一次设置的重生点复活，例如床或重生锚。如果该床或重生锚被破坏、没电或被阻挡，则会回到世界出生点。这个机制的问题是：玩家曾经设置过多个合法重生点，但原版只保留最近一次设置的那个，缺少选择权。因此我希望开发一个模组，为每个玩家维护一个“重生点列表”，并在玩家死亡后提供一个界面，让玩家从仍然有效的重生点中选择复活位置。

请你基于 Fabric 1.20.1 完成该模组的完整开发方案与代码实现。

一、核心功能需求

1. 为每个玩家维护一个独立的重生点列表

每个玩家应该拥有自己的重生点列表，列表中保存该玩家曾经设置过的所有有效重生点。

每个重生点至少包含以下信息：

- 唯一 ID，例如 UUID
- 显示名称，例如“主世界的床 1”“下界重生锚 2”
- 维度 ID，例如 minecraft:overworld、minecraft:the_nether、minecraft:the_end
- 方块坐标 BlockPos
- 重生朝向 yaw / pitch
- 重生点类型：
  - BED
  - RESPAWN_ANCHOR
  - COMMAND
  - CUSTOM
- 创建时间或最近更新时间
- 是否仍然有效
- 如果是重生锚，需要记录或检查其是否还有充能

注意：
不要试图直接让原版玩家对象保存多个 spawnPoint。原版玩家只保存当前一个出生点，本模组需要维护自己的多重生点数据结构。

2. 监听玩家设置重生点的行为

需要支持以下来源：

A. 玩家睡床设置重生点

当玩家成功通过床设置出生点时，将该床位置加入该玩家的重生点列表。

要求：
- 如果该床已经在列表中，不重复添加，而是更新名称、时间、朝向等信息。
- 如果同一位置已有记录，应覆盖旧记录。
- 如果玩家多次在不同床睡觉，应保留多个床重生点，而不是像原版一样只保留最近一个。

B. 玩家使用重生锚设置重生点

当玩家在下界使用已充能的重生锚设置出生点时，将该重生锚位置加入该玩家的重生点列表。

要求：
- 如果该重生锚已经在列表中，不重复添加。
- 如果重生锚被破坏或没有充能，之后需要判定为无效。
- 重生锚仍应遵循原版的维度限制，即主要用于下界。

C. 命令手动添加重生点

实现调试命令：

/multirespawn add <name>
/multirespawn list
/multirespawn remove <id 或 name>
/multirespawn clear

用于测试和管理玩家自己的重生点列表。

3. 删除或过滤已失效重生点

重生点失效时应从玩家列表中删除，或者至少在选择界面中隐藏并提示无效。

失效情况包括：

- 床方块被破坏
- 床所在位置不再是床
- 重生锚被破坏
- 重生锚没有充能
- 所在维度不存在
- 附近找不到安全落点
- 方块被阻挡，玩家无法安全生成
- 位置在虚空、岩浆、不安全区域

要求：
- 玩家死亡打开选择界面之前，先对所有重生点进行校验。
- 无效点从列表中移除，或标记为无效。
- 如果所有重生点都无效，则走原版默认重生逻辑，或者回世界出生点。

4. 玩家死亡后显示重生点选择界面

当玩家死亡后，不应直接只使用原版最近一次重生点。

目标体验：

玩家死亡
→ 出现死亡界面
→ 点击“选择重生点”或直接显示本模组的重生点选择界面
→ 界面列出所有有效重生点
→ 玩家点击一个重生点
→ 服务端验证该重生点是否仍然有效
→ 如果有效，玩家在该位置复活
→ 如果无效，提示玩家并刷新列表

界面中每个重生点建议显示：

- 名称
- 维度
- 坐标
- 类型：床 / 重生锚 / 命令 / 自定义
- 状态：有效 / 无效
- 选择按钮
- 删除按钮，可选

5. 重生流程设计

优先实现方案：

方案 C：死亡界面中选择重生点，然后再执行真正重生。

要求：
- 客户端需要自定义死亡界面或在原版 DeathScreen 上增加按钮。
- 客户端向服务端请求该玩家的有效重生点列表。
- 服务端返回重生点列表。
- 客户端显示 RespawnChoiceScreen。
- 玩家选择某个重生点后，客户端发送选择结果给服务端。
- 服务端验证选择结果。
- 服务端记录 pendingRespawnChoice。
- 玩家执行真正重生。
- 在服务端重生流程中，将原版重生目标替换为玩家选择的重生点。

如果方案 C 实现难度过高，可以先实现降级方案：

玩家点击原版重生
→ 玩家先按原版逻辑复活
→ 立刻打开选择界面
→ 玩家选择重生点
→ 服务端传送玩家到目标位置
→ 给玩家短暂无敌或抗性保护

但最终目标仍然是方案 C。

二、技术实现要求

1. 开发环境

- Minecraft 1.20.1
- Fabric Loader
- Fabric API
- Yarn mappings
- Java 17
- Gradle Loom

2. 推荐包结构

请按照清晰的结构组织代码，例如：

com.example.multirespawn
├── MultiRespawnMod.java
├── MultiRespawnClient.java
├── data
│   ├── RespawnPoint.java
│   ├── RespawnPointType.java
│   ├── PlayerRespawnData.java
│   └── RespawnDataStorage.java
├── event
│   ├── RespawnPointCaptureEvents.java
│   ├── PlayerDeathEvents.java
│   └── PlayerRespawnEvents.java
├── command
│   └── MultiRespawnCommand.java
├── network
│   ├── ModPackets.java
│   ├── RequestRespawnPointsC2SPacket.java
│   ├── SyncRespawnPointsS2CPacket.java
│   └── ChooseRespawnPointC2SPacket.java
├── client
│   └── RespawnChoiceScreen.java
├── respawn
│   ├── RespawnValidator.java
│   ├── RespawnTeleporter.java
│   └── PendingRespawnChoiceManager.java
└── mixin
    ├── DeathScreenMixin.java
    ├── ServerPlayerEntityMixin.java
    └── ServerPlayNetworkHandlerMixin.java

3. 数据持久化

需要将每个玩家的重生点列表保存到世界存档中，服务器重启后仍然存在。

可以使用以下方案之一：

- PersistentState
- 每个玩家单独 NBT
- Cardinal Components API，如果你认为更合适

优先使用不依赖额外大型库的方案。

数据保存格式应支持：

- 玩家 UUID
- 玩家所有重生点
- 每个重生点的维度、坐标、类型、名称、创建时间等信息

4. 玩家数据复制

由于玩家死亡重生后，服务端可能创建新的 ServerPlayerEntity 实例，需要确保玩家的重生点数据不会因为重生而丢失。

要求：
- 使用 Fabric API 的 ServerPlayerEvents.COPY_FROM 或等效方式，在旧玩家数据复制到新玩家时保留本模组数据。
- 使用 ServerPlayerEvents.AFTER_RESPAWN 或等效方式，在玩家重生后执行必要的同步逻辑。

5. 安全落点搜索

不能直接把玩家传送到床或重生锚方块坐标。

需要实现安全落点搜索：

输入：
- ServerWorld world
- BlockPos anchorPos

输出：
- Optional<Vec3d> safeRespawnPos

基本规则：
- 脚部方块位置必须无碰撞
- 头部方块位置必须无碰撞
- 脚下方块必须可站立
- 不能是岩浆
- 不能在虚空
- 不能把玩家卡进方块
- 尽量扫描 anchorPos 周围一定范围，例如 5x5x3 或 7x7x4

6. 重生点验证

实现 RespawnValidator：

validate(ServerPlayerEntity player, RespawnPoint point)

需要返回：
- 是否有效
- 如果无效，原因是什么
- 如果有效，安全落点在哪里

验证逻辑：

BED:
- 检查维度存在
- 检查该位置是否仍然是床
- 检查附近是否有安全落点

RESPAWN_ANCHOR:
- 检查维度存在
- 检查该位置是否仍然是重生锚
- 检查是否有充能
- 检查附近是否有安全落点
- 成功使用后应消耗一次充能，行为尽量接近原版

COMMAND / CUSTOM:
- 检查维度存在
- 检查附近是否有安全落点

7. 网络通信

需要实现客户端和服务端之间的数据交互：

C2S:
- 请求重生点列表
- 选择某个重生点
- 删除某个重生点，可选

S2C:
- 同步重生点列表到客户端
- 打开重生点选择界面
- 返回错误提示，例如“该重生点已失效”

所有选择必须以服务端验证为准。客户端只负责显示，不可信任客户端传来的坐标。

8. 客户端界面

实现 RespawnChoiceScreen。

界面要求：
- 标题：选择重生点
- 每个有效重生点显示一行
- 显示名称、维度、坐标、类型
- 每行有“重生”按钮
- 如果没有有效重生点，显示“没有可用重生点，使用世界出生点”
- 支持返回原版重生逻辑，或提供“世界出生点”按钮
- 不要让客户端直接传送玩家，必须发送选择结果给服务端

9. Mixin 要求

如果 Fabric API 事件不足以实现方案 C，请使用 Mixin。

可能需要 Mixin 的位置：

- DeathScreen：替换或增强原版死亡界面
- ServerPlayerEntity：修改或注入 getRespawnTarget 相关逻辑
- ServerPlayNetworkHandler：拦截客户端重生请求，确保玩家选择已被服务端记录
- PlayerManager / PlayerList 等重生逻辑相关类，如果 1.20.1 中实际调用链需要

要求：
- 尽量减少 Mixin 范围
- 优先使用 @Inject，其次才使用 @Redirect / @ModifyReturnValue
- 每个 Mixin 都要说明注入点和目的
- 避免破坏其他模组兼容性

三、行为规则

1. 玩家死亡时，如果有多个有效重生点

显示选择界面，让玩家选择。

2. 玩家死亡时，如果只有一个有效重生点

可以配置为：
- 直接使用该重生点
- 或仍然显示选择界面

先默认直接使用唯一有效重生点。

3. 玩家死亡时，如果没有有效重生点

使用原版默认重生逻辑或世界出生点。

4. 床或重生锚被破坏后

对应重生点应被移除或标记为无效。

可以在以下时机清理：
- 玩家死亡打开选择界面前
- 玩家选择该重生点时
- 方块破坏事件发生时
- 服务器定期清理，可选

5. 重生锚消耗规则

如果玩家选择重生锚重生：
- 检查充能
- 成功重生后消耗 1 点充能
- 如果充能变为 0，该锚可以保留在列表但下次验证失败，或者直接移除

四、需要输出的内容

请你完成以下输出：

1. 总体架构说明
解释这个模组的数据流、事件流、客户端/服务端职责划分。

2. 开发环境搭建
给出 Fabric 1.20.1 项目创建方式、build.gradle、fabric.mod.json、入口类配置。

3. 数据结构设计
给出 RespawnPoint、RespawnPointType、PlayerRespawnData、RespawnDataStorage 的完整代码或接近完整代码。

4. 事件监听实现
实现玩家睡床、使用重生锚、死亡、重生、数据复制相关逻辑。

5. 命令实现
实现 /multirespawn add/list/remove/clear 命令。

6. 网络包实现
实现 C2S 和 S2C packet，用于请求列表、同步列表、选择重生点。

7. 客户端 Screen 实现
实现 RespawnChoiceScreen，至少能显示列表并点击选择。

8. 重生点验证与传送
实现 RespawnValidator 和 RespawnTeleporter。

9. Mixin 实现
如果方案 C 需要 Mixin，请给出 Mixin 类、mixin json 配置、注入点说明。

10. 测试流程
给出详细测试用例，包括：

- 主世界设置多个床
- 下界设置多个重生锚
- 主世界死亡后选择下界重生锚
- 下界死亡后选择主世界床
- 床被破坏后是否从列表删除
- 重生锚没电后是否不可选择
- 服务器重启后数据是否保留
- 多玩家数据是否互不影响
- 玩家退出重进后数据是否正常
- 没有有效重生点时是否回世界出生点

11. 注意事项
说明可能的兼容性问题、Mixin 风险、客户端/服务端同步问题、跨维度传送问题。

五、开发约束

- 请不要只给概念说明，要尽量给可运行代码。
- 所有代码都以 Minecraft 1.20.1 + Fabric 为目标。
- 不要使用 Forge / NeoForge API。
- 不要依赖 Bukkit / Spigot / Paper API。
- 如果某些类名在 Yarn 1.20.1 中可能不同，请明确说明需要根据 IDE 中的 Yarn 映射确认。
- 如果某个功能需要 Mixin，请明确说明为什么事件无法完成。
- 代码要有注释，方便初学者理解。
- 先保证最小可用版本，再考虑优化。
- 如果方案 C 太复杂，请先实现“原版重生后立即打开选择界面并传送”的可运行降级版本，但代码结构要为后续方案 C 留接口。